name: db-migrate

# DESACTIVE — declenchement manuel uniquement (voir avertissement ci-dessous).
on:
  workflow_dispatch:

jobs:
  migrate:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: ATTENTION — workflow retire, ne pas relancer sans relire ce fichier
        run: |
          echo "::error::Ce workflow etait DESTRUCTEUR : il faisait DROP SCHEMA public CASCADE"
          echo "::error::puis ne reappliquait que supabase/migrations/001_initial_schema.sql,"
          echo "::error::qui est un schema PERIME (ancien pro_profiles/missions/litiges)."
          echo "::error::Declenche automatiquement a chaque push touchant supabase/migrations/**,"
          echo "::error::il aurait efface le vrai schema de production (profiles/provider_profiles/"
          echo "::error::transactions/...) pour le remplacer par l'ancien schema, cassant tout le site."
          echo "::error::Trouve casse le 13/08/2026 (echec avant la partie destructrice, base intacte)."
          echo "::error::Neutralise : declenchement push retire, corps du job vide."
          echo "::error::Les migrations sont desormais appliquees directement via le connecteur"
          echo "::error::Supabase (MCP), en revue une par une, puis documentees dans supabase/migrations/"
          echo "::error::pour que le depot reste source de verite en lecture. Ne pas reintroduire"
          echo "::error::d'application automatique de schema sans un vrai systeme de migrations"
          echo "::error::incrementales (table de suivi des migrations deja appliquees)."
          exit 1
